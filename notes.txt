- Web dashboard.
- Mlab (15 nodes)= Workstations, rasberrypis, mobiles

[ Workstation Daemon ] ─┐
                        ├──> [ Backend API ] ───> [ Database ]
[ Mobile App ] ────────┘            │
                                     └──> [ Dashboard (Web UI) ]
                                     
KPIs = voltage, freq, cpu utilization, temperature, Power consumption (W), Energy consumption (Wh), RAM utilization,
Node uptime. 

Components
    Data producers
        Workstation daemon/ Mobile app → collects energy KPIs
    Backend
        REST API (or gRPC)
        Auth + validation
        Aggregation logic
    Database
        Time-series optimized (Benchmark 5)
    Dashboard
        Charts + filtering
        Efficient querying